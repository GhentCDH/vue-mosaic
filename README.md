# Vue Mosaic

Vue Mosaic is a Tiling Window Manage for Vue. It provides a simple and flexible API to tile arbitrarily complex Vue components across a user's view. Vue-mosaic is written in TypeScript and based on react-mosaic, a similar library for React.

![img.png](img.png)